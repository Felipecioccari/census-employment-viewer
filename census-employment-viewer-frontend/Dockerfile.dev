FROM node:20-alpine AS dev
WORKDIR /app

# Install deps (including dev)
COPY package*.json ./
RUN npm install

# Copy source (for first build)
COPY . .

# Default command for development
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]